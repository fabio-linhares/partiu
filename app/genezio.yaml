frontend:
    path: ./
    publish: bin
    scripts:
        build: uvicorn api:app --host 0.0.0.0 --port 8000 --reload
        deploy: pip install
    subdomain: partiu
name: partiu
region: us-east-1
yamlVersion: 2
